
在Kotlin协程中，虽然协程本身是轻量级的并发单元，但它们仍然可能在多线程环境中运行。因此，确保数据的一致性和线程安全性仍然是必要的。以下是一些在协程中实现线程安全的常用方法：

1. 使用Mutex（互斥锁）
2. 使用Channel
3. 使用ThreadLocal
4. 使用原子变量

尽量不使用synchronized
synchronized 是 JVM 的阻塞式互斥锁：拿不到锁时，当前运行该协程的线程会被阻塞，直到获得锁为止。
