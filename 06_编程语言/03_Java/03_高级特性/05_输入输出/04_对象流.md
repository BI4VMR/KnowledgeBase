<!-- TODO
# 简介
序列化

我们通常不会使用Java自带的序列化机制，它是Java专用的，无法被其他语言编写的程序识别，通用性差。

我们常用以下通用格式进行序列化与反序列化，以便跨平台交换数据。

- JSON
- XML
- ProtoBuf

该类必须实现java.io.Serializable 接口，否则会抛出NotSerializableException 。
    该类的所有字段都必须是可序列化的。如果一个字段不需要序列化，则需要使用transient 关键字进行修饰。

-->

