<!-- TODO

启动activity
am start --display <display_id> 参数在 Kotlin 中等价于使用 ActivityOptions.setLaunchDisplayId(displayId) 设置启动选项，并将其 Bundle 传递给 startActivity()。
Kotlin

import android.app.ActivityOptions
import android.content.Context
import android.content.Intent

fun startActivityOnDisplay(context: Context, targetDisplayId: Int) {
    val intent = Intent(context, TargetActivity::class.java)

    // API 26 (Android 8.0) 及以上支持 setLaunchDisplayId
    val options = ActivityOptions.makeBasic().apply {
        launchDisplayId = targetDisplayId // 等价于 setLaunchDisplayId(targetDisplayId)
    }

    context.startActivity(intent, options.toBundle())
}
-->
