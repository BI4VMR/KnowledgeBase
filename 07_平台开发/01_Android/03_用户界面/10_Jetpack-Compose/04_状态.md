# 简介
应用的“状态”是指可以随时间变化的任何值。这是一个非常宽泛的定义，从 Room 数据库到类的变量，全部涵盖在内。

注意：优秀实践是为所有可组合函数提供默认的 Modifier，从而提高可重用性。它应作为第一个可选参数显示在参数列表中，位于所有必需参数之后。


本章的示例工程详见以下链接：

- [🔗 示例工程：概述](https://github.com/BI4VMR/Study-Android/tree/master/M03_UI/C10_Compose/S01_Base)

# 基本应用
下文示例展示了Jetpack Compose的基本使用方法。

🔴 示例一：状态。

在本示例中，我们为工程添加Jetpack Compose的相关配置，并创建基本的UI组件。

第三步，我们创建一个Activity，声明一些Compose组件，并将它们作为Activity的显示内容。

"TestUIBase.kt":

```kotlin
@Composable
fun TaskInfo() {
    // 声明状态变量，每当状态变量被改变时，该Composable函数将被运行环境调用重绘UI。
    val count: MutableState<Int> = remember { mutableStateOf(0) }

    Column {
        // 读取状态变量
        Text("待办事项：${count.value}")
        Row {
            // 按钮被点击后修改状态变量的值
            Button(onClick = { count.value++ }) {
                Text("增加")
            }
            Button(onClick = { count.value-- }) {
                Text("减少")
            }
        }
    }
}
```

在上述代码中， 


为了让 Compose 能够感知到状态变化，状态的值需要包装到一个State对象里。Jetpack Compose 提供的mutableStateOf()函数就能帮我们完成这个包装操作。这个函数会返回一个MutableState<T>实例，Compose 会跟踪这个实例的变化，在值被修改时进行 UI 更新。 

var enabled by remember { mutableStateOf(true) }

mutableStateOf

用于创建MutableState，如果写为 var enabled by remember { true }
则是普通变量，不具有可观察性，修改后监听enabled的变量也不会刷新


特定类型

mutableIntStateOf
mutableLongStateOf
mutableFloatStateOf
mutableDoubleStateOf



remember
重组时记住状态

如果写为 var enabled by mutableStateOf(true)  ，修改不会生效，

状态变更重组，enable未被记住，创建新对象且初始值为true

remember：处理局部临时状态（如动画进度）,局部重组时保留，但旋屏等导致整个界面重建会被清除。
rememberSaveable：整个界面重组也需要保留的状态，如用户输入、选项卡选中状态等，此类状态也可以放在viewmodel中，已livedata或stateflow表示，viewmodel生命周期不会受界面旋转影响。


by
将enabled的get/set委托，此时变量类型为MutableState的内容boolean，其他组件可以直接访问，
该部分不是必要的，如果写为 var enabled = remember { mutableStateOf(true) }
可以正常工作，但 enabled 类型变为 MutableState，需要调用者 enabled.value来读取，相对于使用属性委托更繁琐。




其他类型
viewmode statflow
collectAsState
自动监听，转为state

val flow = MutableStateFlow("")
// ...
val state by flow.collectAsState()

// for lifecycle aware version
val state by flow.collectAsStateWithLifecycle()

viewmodel livedata

collectAsState
自动监听，转为state


dependencies {
    implementation "androidx.compose.runtime:runtime-livedata:x.y.z"
}

val liveData = MutableLiveData<String>()
// ...
val state by liveData.observeAsState()



callback

现有业务提供callback接口，可以这样转换




# 业务方法

声明式基于不可变对象感知变化，不能使用可变对象，不能原地修改变量

理想情况
student(id name score)
val a = student
state = student

state.value = student.copy(score = 100)


student是data class，我们更新状态时调用copy方法创建新对象并修改name属性，此时对象变更，状态能够感知变化并重组。


错误用法

student(id name var score)
val a = student
state = student


a.score = 100
state.value = student

修改后将对象赋值给state，此时界面不会重组，因为初始化时状态指向变量，执行 a a.score = 100时，该对象属性被修改，state也被修改，再执行
state.value = student语句实际上并没有任何作用，因此不会更新。

新代码都应当使用data class与不可变属性构建对象，并通过copy新建对象并赋值属性的方式修改，对于已有的使用了var class的系统，应当手动实现新建对象并copy属性的操作，或将其封装为dataclass，不能直接赋值。

## 副作用
非ui状态的操作

网页/API 数据请求

弹出一个 Toast 或 Snackbar

注册一个传感器/广播监听器

跳转页面或修改全局变量

重组不可预期，如果直接将此类操作写在compsoe方法内，将会频繁刷新，文本框输入文本，输入一个字刷新一次。


副作用：每当key改变时执行回调，可以执行协程

LaunchedEffect(key) {
    ...
}

key可以有多个，任意key变化都会触发，可以实现级联更新。

组件初始化时必然会执行一次

如果希望组件初始化时运行一次，不需要跟随状态变化，key可以传入Unit。



副作用：每当key改变时执行onDispose回调进行清理，再执行onDispose之前的语句，此处不能执行协程

DisposableEffect(key) {
    ...

    onDispose {
        // 清理操作
    }
}

组件初始化时必然会执行一次onDispose前的语句。
