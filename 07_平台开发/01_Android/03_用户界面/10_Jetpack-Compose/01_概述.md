# 简介
Jetpack Compose 是一种用于构建用户界面的框架，基于响应式编程模型与 Kotlin 函数式语法，通过 Skia 图形引擎绘制图像。与传统的 View 体系相比，语法更为简洁、性能更高。

在 View 体系中，我们需要监听外部事件（例如：回调方法），并在事件发生时控制相关 UI 组件作出响应（例如：更新文本内容、控制图片透明度等）；在 Compose 体系中，我们通过函数描述 UI 组件，将可变属性（例如：文本内容、文本颜色等）作为函数的参数，每当外部状态变化时， Compose 运行环境将会自动调用受影响组件的声明函数，传入新的参数以重新绘制界面。

本章的相关知识详见以下链接：

- [🔗 Android官方文档 - Jetpack Compose](https://developer.android.com/develop/ui/compose/documentation)
- [🔗 Android官方示例 - Jetpack Compose](https://developer.android.com/courses/pathways/compose)

本章的示例工程详见以下链接：

- [🔗 示例工程：概述](https://github.com/BI4VMR/Study-Android/tree/master/M03_UI/C10_Compose/S01_Base)


# 基本应用
## 早期版本
在较新版本（组件版本 `1.6.8` 、 BOM 版本 `2024.06.00` 及更高版本）中， Gradle 配置出现了变化，该部分将在后文章节 [🧭 较新版本](#较新版本) 中介绍。

🔴 示例一：构建 Jetpack Compose 环境。

在本示例中，我们为工程添加 Jetpack Compose 的相关配置，并创建基本的示例界面。

第一步，在模块的 Gradle 配置文件中添加对 Jetpack Compose 组件的依赖声明。

`build.gradle` :

```groovy
dependencies {
    // Jetpack Compose BOM 版本配置
    implementation platform('androidx.compose:compose-bom:2023.06.01')

    // Jetpack Compose 核心组件
    implementation 'androidx.compose.ui:ui'
    // 仅包含 `@Preview` 注解声明，需要在所有 Build Variant 引入确保编译通过。
    implementation 'androidx.compose.ui:ui-tooling-preview'
    // 2D 画布等基础 API ，被 `ui` 组件依赖，无需手动声明，仅编写基础库时需要。
    implementation 'androidx.compose.ui:ui-graphics'
    // 基础控件，被 `material3` 组件依赖，无需手动声明，仅编写基础库时需要。
    implementation 'androidx.compose.foundation:foundation'
    // Material2 控件
    implementation 'androidx.compose.material:material'
    // Material3 控件
    implementation 'androidx.compose.material3:material3'
    // Activity 支持
    implementation 'androidx.activity:activity-compose:1.6.1'

    // Jetpack Compose 调试工具
    debugImplementation 'androidx.compose.ui:ui-tooling'

    // Jetpack Compose 测试工具
    androidTestImplementation(platform('androidx.compose:compose-bom:2023.06.01'))
    androidTestImplementation 'androidx.compose.ui:ui-test-junit4'
    androidTestImplementation 'androidx.compose.ui:ui-test-manifest'
}
```

上述内容也可以使用 Kotlin 语言编写：

`build.gradle.kts` :

```kotlin
dependencies {
    // Jetpack Compose BOM 版本配置
    implementation(platform("androidx.compose:compose-bom:2023.06.01"))

    // Jetpack Compose 核心组件
    implementation("androidx.compose.ui:ui")
    // 仅包含 `@Preview` 注解声明，需要在所有 Build Variant 引入确保编译通过。
    implementation("androidx.compose.ui:ui-tooling-preview")
    // 2D 画布等基础 API ，被 `ui` 组件依赖，无需手动声明，仅编写基础库时需要。
    implementation("androidx.compose.ui:ui-graphics")
    // 基础控件，被 `material3` 组件依赖，无需手动声明，仅编写基础库时需要。
    implementation("androidx.compose.foundation:foundation")
    // Material2 控件
    implementation("androidx.compose.material:material")
    // Material3 控件
    implementation("androidx.compose.material3:material3")
    // Activity 支持
    implementation("androidx.activity:activity-compose:1.6.1")

    // Jetpack Compose 调试工具
    // IDE 预览能力，通过 `debugImplementation` 引入即可。
    debugImplementation("androidx.compose.ui:ui-tooling")

    // Jetpack Compose 测试工具
    androidTestImplementation(platform("androidx.compose:compose-bom:2023.06.01"))
    androidTestImplementation("androidx.compose.ui:ui-test-junit4")
    androidTestImplementation("androidx.compose.ui:ui-test-manifest")
}
```

Jetpack Compose 组件数量众多，若由开发者手动声明每个组件的版本号，容易出现冲突；因此官方提供了 BOM 文件，当我们引入该文件后，声明其他组件时就不必再指明版本号了。 BOM 文件版本与 Compose 组件版本的对应关系可参考相关网页： [🔗 Jetpack Compose BOM版本](https://developer.android.com/develop/ui/compose/bom/bom-mapping) 。

Jetpack Compose 的核心组件是必选的；调试工具是可选的，它们能够在开发阶段提供预览等功能，不必在发布阶段引入；测试工具则提供了 JUnit 支持，如果我们不需要编写测试代码，可以不声明这些依赖项。

第二步，在模块的 Gradle 配置文件中添加 Jetpack Compose 的相关配置。

`build.gradle` :

```groovy
android {
    buildFeatures {
        // 开启Compose
        compose = true
    }

    composeOptions {
        // Jetpack Compose注解处理器版本
        kotlinCompilerExtensionVersion = '1.5.7'
    }
}
```

上述内容也可以使用 Kotlin 语言编写：

`build.gradle.kts` :

```kotlin
android {
    buildFeatures {
        // 开启Compose
        compose = true
    }

    composeOptions {
        // Jetpack Compose注解处理器版本
        kotlinCompilerExtensionVersion = "1.5.7"
    }
}
```

我们首先需要在 `buildFeatures {}` 小节中开启 Jetpack Compose 编译功能，然后在 `composeOptions {}` 小节中指明编译器的版本号。编译器版本应与当前模块的 Kotlin 版本相匹配，对应关系可在 [🔗 Compose编译器版本](https://developer.android.com/jetpack/androidx/releases/compose-kotlin#pre-release_kotlin_compatibility) 网页中查询。

第三步，创建测试 Activity ，声明一些 Compose 组件，并将它们作为 Activity 的显示内容。

`TestUIBase.kt` :

```kotlin
class TestUIBase : ComponentActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // 显示 Compose UI 组件
        setContent {
            // 应用主题
            TestComposeTheme {
                // 应用布局
                Scaffold(modifier = Modifier.fillMaxSize()) { innerPadding ->
                    // 放置控件
                    Greeting(
                        name = "Android",
                        modifier = Modifier.padding(innerPadding)
                    )
                }
            }
        }
    }

    // 声明 Compose 组件
    @Composable
    fun Greeting(name: String, modifier: Modifier = Modifier) {
        // 声明文本框
        Text(
            text = "Hello $name!",
            modifier = modifier
        )
    }

    // 声明 Compose 组件预览
    @Preview(showBackground = true)
    @Composable
    fun GreetingPreview() {
        TestComposeTheme {
            Greeting("Android")
        }
    }
}
```

在上述代码中， `Greeting()` 方法被 `@Composable` 注解修饰，因此这是一个 Compose 组件；其中的 `Text()` 方法声明了一个文本框，部分内容由 `Greeting()` 方法的第一参数 `name` 决定，以提供动态设置显示内容的能力。

`GreetingPreview()` 方法被 `@Preview` 注解修饰，在开发阶段 Android Studio 能够提供组件外观预览，在运行阶段则无作用。

在 Activity 的生命周期方法 `onCreate()` 中，我们使用 `setContent()` 方法设置界面内容；最外层的 TestComposeTheme 是主题、较内层的 Scaffold 是根布局、最内层的 Greeting 即自定义 Compose 组件。

## 较新版本
自从 Jetpack Compose 1.6.8 （或 BOM 版本号 `2024.06.00` ）开始， Compose 编译器变更为一个独立的 Gradle 插件，且版本号跟随工程的 Kotlin 版本、不再通过 `composeOptions {}` 小节声明。

`build.gradle` :

```groovy
plugins {
    // Kotlin-Android 插件的版本号既工程的 Kotlin 版本号。
    id 'org.jetbrains.kotlin.android' version '2.4.10' apply false
    // Compose 编译器插件，版本号跟随 Kotlin 版本。
    id 'org.jetbrains.kotlin.plugin.compose' version '2.4.10' apply false
}
```

上述内容也可以使用 Kotlin 语言编写：

`build.gradle.kts` :

```kotlin
plugins {
    // Kotlin-Android 插件的版本号既工程的 Kotlin 版本号。
    id("org.jetbrains.kotlin.android").version("2.4.10").apply(false)
    // Compose 编译器插件，版本号跟随 Kotlin 版本。
    id("org.jetbrains.kotlin.plugin.compose").version("2.4.10").apply(false)
}
```

若我们为较旧的版本进行升级，需要为工程新增 Compose 编译器插件声明，并移除 `composeOptions {}` 小节中的编译器版本声明。


# 数据交互
在前文“示例一”中，我们已了解 Compose 的基本使用方法，但仅实现了无功能的静态界面；在实际应用中，我们需要建立控件与状态的联系，使控件监听数据变化，并处理用户点击按钮等事件。

🟠 示例二：计数器。

在本示例中，我们通过 Compose 实现一个计数器，初始数值为 `0` ，用户可以点击按钮增减数值。

第一步，组合 Compose 控件，利用文本框显示当前数值，利用按钮与回调方法通知外部组件数值变化事件。

`TestUIState.kt` :

```kotlin
@Composable
fun CounterStateless(value: Int, onAdd: () -> Unit, onReduce: () -> Unit) {
    Column {
        // 文本框：显示当前数值
        Text("当前数值：[$value]")
        // 增加按钮：被点击时执行 `onAdd()` 函数
        Button(onClick = onAdd) {
            Text("数值增加")
        }
        // 减少按钮：被点击时执行 `onReduce()` 函数
        Button(onClick = onReduce) {
            Text("数值减少")
        }
    }
}
```

CounterStateless 组件仅根据外部传入的参数更新显示内容，并将按钮点击事件通过回调方法导出，函数内部并未持有任何状态变量。此类函数被 Compose 运行时多次执行后不会影响其他组件，因此我们将其称为“无状态组件”，它们可被跨工程复用。

第二步，编写另一个控件，使 CounterStateless 和 Compose 的状态变量建立联系。

`TestUIState.kt` :

```kotlin
@Composable
fun Counter() {
    Column {
        // 声明状态：表示当前数量
        var count: Int by remember { mutableIntStateOf(0) }

        Text("计数器")
        // 组装控件，传入当前数量，并在回调函数中更新数量。
        CounterStateless(
            count,
            onAdd = { count++ },
            onReduce = { count-- }
        )
    }
}
```

Counter 组件中的 `count` 是 Compose 状态变量，每当 `count` 内部的整型数值发生变化， Compose 运行时将会重新执行 CounterStateless 函数并传入当前数值，使界面显示最新数量，该过程被称为“重组 (Recomposition) ”。

`Text("计数器")` 控件并未引用任何状态变量，当 Counter 发生重组时该控件不会被更新。在响应式框架中，我们只需声明每个组件关心的状态，即可实现局部更新，不必像命令式框架一样在事件回调中指明需要更新的控件。

Counter 组件持有状态变量，与特定业务耦合，因此我们将其称为“有状态组件”，此类组件不具有通用性，无法跨工程复用。部分组件较为特殊，它们持有状态变量，但仅用于记录菜单或弹窗的展开与收起状态，与具体业务无关，我们仍然将此类组件视为可复用的。

此时运行示例程序，并查看界面外观：

<div align="center">

![示例程序](./Assets_概述/数据交互_示例程序.gif)

</div>


# 疑难解答
## 索引

<div align="center">

|       序号        |                           摘要                           |
| :---------------: | :------------------------------------------------------: |
| [案例一](#案例一) | 编译失败，错误消息为：部分组件仅支持 API 34 及更高版本。 |

</div>

## 案例一
### 问题描述
APP模块编译失败，错误信息为： `<组件> depend on it to compile against version 34 or later of the Android APIs` 。

```text
22 issues were found when checking AAR metadata:

1.  Dependency 'androidx.compose.material3:material3-android:1.2.1' requires libraries and applications that depend on it to compile against version 34 or later of the Android APIs.

# 此处省略部分输出内容...
```

### 问题分析
部分组件的较新版本不兼容 API 33 及更低版本的 SDK ，如果我们无法升级工程的编译 SDK 版本，则只能使用这些组件的较旧版本。

### 解决方案
目前各个组件对 API 33 及更高版本的支持情况尚无官方文档可供查询，经过测试，当工程的 API 版本为 33 时，各个组件能够支持的最高版本如下文列表所示：

- `androidx.compose:compose-bom` : `2023.06.01`
- `androidx.activity:activity-compose` : `1.6.1`
