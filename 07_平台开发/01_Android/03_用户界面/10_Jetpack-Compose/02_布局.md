<!-- TODO 
# 简介

注意：优秀实践是为所有可组合函数提供默认的 Modifier，从而提高可重用性。它应作为第一个可选参数显示在参数列表中，位于所有必需参数之后。


本章的示例工程详见以下链接：

- [🔗 示例工程：布局](https://github.com/BI4VMR/Study-Android/tree/master/M03_UI/C10_Compose/S02_Layout)


# 全局属性
## 宽高约束

默认规则：尺寸与控件内容相同，类似于XML 中的wrap_content

固定宽高：
可以使用Modifier指定宽高

size(size: Dp) : 将控件的宽度与高度都设为 `size` 指定的值。
size(width: Dp, height: Dp) : 指定控件的宽度与高度。
width(width: Dp) : 指定控件的宽度。
height(height: Dp) : 指定控件的高度。


充满父布局：
类似于 View 中的 Matchparent

.fillMaxHeight() //填满高度
.fillMaxWidth() //填满宽度
.fillMaxSize()//填满宽高


.fillMaxHeight(0.5f) //填满父容器的1/2高度

该属性会使父布局尺寸改变。

错误用法：两个子元素都用fillMaxHeight，这会导致第一个元素就占满整个父容器，第二个元素挤出容器可见范围。



wrapcontent
width(IntrinsicSize.Min)
height(IntrinsicSize.Min)
Modifier.wrapContentSize()

通常不需要显式配置wrapContentSize，默认行为就是如此，如果不生效，需要检查容器内的子项是否具有fillMaxSize/weight=1等属性，HorizontalDivider默认撑满当前方向，如果需要手动指定，需要显式设置为wrapContentSize。


最小宽高
.defaultMinSize(minWidth = 50.dp, minHeight = 50.dp)

sizeIn(
    minWidth: Dp = Dp.Unspecified,
    minHeight: Dp = Dp.Unspecified,
    maxWidth: Dp = Dp.Unspecified,
    maxHeight: Dp = Dp.Unspecified
)


无视父容器限制
requiredHeight(10000)

## 边距
### 基本应用

.padding(5.dp)
.padding(horizontal = 20.dp)
.padding(vertical = 10.dp)
.padding(0.dp,5.dp,50.dp,0.dp)


```kotlin
Text(
    text = "我能吞下玻璃而不伤身体。",
    modifier = Modifier.padding(50.dp)
)
```

当我们为 Text 的 Modifier 添加 `padding(50.dp)` 语句后，文本内容四周距离外层容器都增加了50DP的边距。



### 配置顺序

修饰符语句的顺序会影响最终效果，较晚出现的语句将继承较早出现的语句所配置的环境。


在上述实例中，我们先设置了灰色背景，然后50边距，最后设置蓝色背景，此时蓝色背景继承先前的50dp边距，仅在指定区域内绘制。

基于这种特性，Compose 中没有 margin 属性，我们需要合理规划各语句的顺序，完成布局。



### 可选属性



```kotlin
@Composable
fun Tab(
    title: String,
    modifier: Modifier = Modifier,
    allowClick: Boolean = true
) {
    val context = LocalContext.current
    // 组件的根布局，继承外部传入的 Modifier 。
    Column(
        modifier = modifier
            .background(Color.Magenta.copy(0.3F), RoundedCornerShape(16.dp))
            // 可选属性，根据 `allowClick` 决定是否添加点击事件。
            .then(
                if (allowClick) Modifier.clickable {
                    Toast.makeText(context, title, Toast.LENGTH_SHORT).show()
                }
                else Modifier
            )
            .padding(8.dp)
    ) {
        Text(title)
    }
}
```

`then()` 方法用于将另一个 Modifier 实例的属性融合到当前 Modifier 。在上文代码中，如果 Tab 控件的调用者指明 `allowClick` 参数为 `true` ，则通过 `Modifier.clickable()` 语句定义的点击事件将合并至调用者传入的 Modifier 中；如果调用者指明 `allowClick` 参数为 `false` ，则合并没有任何属性的 Modifier 实例，未改变调用者传入的原始 Modifier 。



# Column 和 Row
## 简介
Column 将子元素依次垂直排列； Row 将子元素依次水平排列，它们是 Compose 中的基础布局组件，能够实现大多数常见的用户界面。

## 基本应用
下文示例展示了 Column 和 Row 的基本使用方法。

🔴 示例一：【标题】。

在本示例中，我们在 Column 和 Row 容器中放置一些控件，观察它们的排列规则。

`TestUIBase.kt` :

```kotlin
Column {
    // 子元素 A
    Text(
        text = "A",
        modifier = Modifier
            .size(50.dp)
            .background(Color.Cyan)
    )
    // 子元素 B
    Text(
        text = "B",
        modifier = Modifier
            .width(100.dp)
            .height(50.dp)
            .background(Color.Red)
    )
    // 子元素 C
    Text(
        text = "C",
        modifier = Modifier
            .width(50.dp)
            .height(75.dp)
            .background(Color.Yellow)
    )
}
```


## 对齐方式
在 Column 和 Row 中，主要布局方向的排列规则被称为 Arrangement ，次要布局方向的对齐方式被称为 Alignment 。以 Row 为例，子元素被水平排列，因此 `horizontalArrangement` 属性用于控制水平方向的对齐方式； `verticalAlignment` 属性则控制垂直方向的对齐方式。



## 比例布局
我们可以在 Column 或 Row 子元素的 Modifier 中添加 `weight(v: Float)` 属性，控制各子元素在父容器中占据控件的比例。
weight属性控制当前控件在父容器中所占比例

c1 weight 0.8F
c2 weight 0.2F

此时c1占据父容器0.8的宽或高 c2占据父容器0.2的宽或高


特殊用法

c1 不设置
c2 weight 1F

先测量没有设置 weight 的组件，再把剩余空间分配给它
此时表示占据所有剩余控件，c1遵循默认规则，尺寸与内容相等，c2 占据剩余的所有空间。



# Box：堆叠布局（类似 FrameLayout），子元素的 Alignment 对齐。

# Scaffold

页面脚手架，用于组装topBar/bottombar/fab等组件，实现符合material设计的页面。



    LazyColumn 与 LazyRow：高性能长列表实现。

    LazyVerticalGrid：网格布局。

    关键点：item 与 items 的 DSL 语法、key 的重要性。

约束布局 (ConstraintLayout)

    在 Compose 中引入 ConstraintLayout 的场景。

    使用 createRefs() 与 constrainAs 建立约束。



-->
