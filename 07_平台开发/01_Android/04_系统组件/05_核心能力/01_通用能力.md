<!-- TODO



# 系统信息

appendLog("BRAND！\n"+ Build.BRAND);
appendLog("ID！\n"+ Build.ID);
appendLog("MODEL！\n"+ Build.MODEL);


## 设备标识符

API 28及更早版本应用可以请求READ_PHONE_STATE获取Build.Serier
API 29开始Build.Serier对普通应用不可用，系统应用需要申请READ_PRIVILEGED_PHONE_STATE权限。


android_id
    Android 8.0以下：卸载重装后会改变
    Android 8.0及以上：基于应用签名+UserID+设备ID生成，相同签名的应用卸载重装后不会改变，可以作为应用级别的标识符。

对于广告应用可以使用可由用户重置的广告专用ID，保护用户隐私。
对于不需要追踪设备的ID，可以由应用写入SP或文件，卸载后ID自动失效。


# 时间设置


//读取
Settings.System.getString(Context.getContentResolver(), Settings.System.TIME_12_24, "12");

//设置
Settings.System.putString(Context.getContentResolver(),Settings.System.TIME_12_24, "24");



//查询：
adb shell settings get system time_12_24

//设置24小时制:

adb shell settings set system time_12_24 24


# 版本兼容性
不同版本的Android提供的接口存在差异，部分接口在新旧版本的行为不同，部分用户可能仍使用旧版系统的设备，我们可以对系统版本进行判断，选择相应的业务代码实现功能。





各个系统版本的变更详情可在 [🔗 Android - 版本变更](https://developer.android.com/about/versions) 查询。


# 进程管理
默认情况下app只有单个进程，activity service等都在该进程运行，有时我们希望将应用设计成多进程架构，提高稳定性，例如UI和逻辑分离，可以在mf中为特地的组件添加 `android:process=":<子进程名>"` ，此时该组件将以独立的进程运行

例如我们为测试activity添加 ``，从示例应用启动该界面，

此时activity是一个独立进程，名为包名:子进程。

四大组件节点都可设置进程，每个进程初始化时都会执行一次application的逻辑，对于某些全局性的逻辑，不能重复执行，我们可以使用AM的接口判断当前进程名字，仅在主进程初始化时执行相关逻辑。

我们可以使用以下方法判断当前进程是否为应用的主进程

ActivityManager的 `List<RunningAppProcessInfo> getRunningAppProcesses()` : 获取当前应用的所有进程。
        Process.killProcess(pid) :杀死当前应用的指定进程
       Process.myPid() ：获取当前进程ID。




# 隐藏接口


@hide 文档注解，声明为隐藏api

@SystemApi  @PrivateApi 普通注解，用于对hide注解进行补充说明。

android源码中有很多hide的接口，编码时无法直接调用. 怎么办呢? 首先明确一点：hide标记只影响编译时，不影响运行时，只要编译能通过，运行时就能正常访问hide标记的接口,因此，只需要确保调用hide接口能编译通过即可，通常有三种做法:

    基于android源码编译
    修改源码，去除hide标记，并编译成framework.jar导入项目中
    反射
    其中方法1，2操作比较复杂，通常很少使用


1. 下载AOSP源码，移除 `@hide` 注解，编译生成新的 `framework.jar` 并导入当前工程。
2. 将需要反射访问的变量或方法所在文件，复制到工程对应包路径下，确保编译通过。
3. 通过反射机制获取变量、方法等。

方法1
方法1只适用于定制化的产品组件，不适用于需要兼容多个版本的库组件，因为每个工程只能导入一个版本的`framework.jar`文件；方法2需要删除源码中多余的部分，




# 注解
@MainThread
标记该方法需要在主线程调用，非强制，不会检查并阻断。





# 版本变更
## 索引

<div align="center">

|       序号        |       版本       |       摘要       |
| :---------------: | :--------------: | :--------------: |
| [变更一](#变更一) | Android 10 | 系统限制应用程序调用SDK中的隐藏接口。 |

</div>

## 变更一
### 摘要
在Android 10对非SDK接口进行了限制，因而@hide注释的方法被列入黑名单，外部不能访问。

### 详情
此处填写【详情】。

### 兼容方案
但是可以通过adb shell settings put global hidden_api_policy 1命令打开权限访问。

通过adb shell settings delete global hidden_api_policy解除设置。（https://developer.android.com/about/versions/10/non-sdk-q）


-->
